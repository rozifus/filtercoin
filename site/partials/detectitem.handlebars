{{# if step }}
    <i class="ui icon filter"></i>
{{/ if }}
<div data-filter-id="{{ step }}" class="remove-filter ui button label">
  {{ name }}
  {{# unless unknown }}
  <div class="detail">
    {{ count }}
  </div>
  {{/ unless }}
</div>

